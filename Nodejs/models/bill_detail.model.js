// const ketnoi = require('../connect-mysql');
// const util = require('node:util');
// const query = util.promisify(ketnoi.query).bind(ketnoi);

// const BillDetail = {
//   createMultiple: async (billDetails) => {
//     const [result] = await ketnoi.query('INSERT INTO bill_detail (bill_id, product_id, quantity, price, subtotal) VALUES ?', [billDetails]);
//     return result;
//   }
// };

// module.exports = BillDetail;
